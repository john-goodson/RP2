import { CellWorkUnitsPipe } from './cell-work-units.pipe';

describe('CellWorkUnitsPipe', () => {
  it('create an instance', () => {
    const pipe = new CellWorkUnitsPipe();
    expect(pipe).toBeTruthy();
  });
});
