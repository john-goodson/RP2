import { ProjectListFilterPipe } from './project-list-filter.pipe';

describe('ProjectListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
