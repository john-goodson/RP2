import { ProjectDateSpanDirective } from './project-date-span.directive';

describe('ProjectDateSpanDirective', () => {
  it('should create an instance', () => {
    const directive = new ProjectDateSpanDirective();
    expect(directive).toBeTruthy();
  });
});
